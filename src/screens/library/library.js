import { Link } from "react-router-dom";

export const Library = () => {
    return(
        <div>
            Library Screen
            <button><Link to="/">Go Back</Link></button>
        </div>
    );
}